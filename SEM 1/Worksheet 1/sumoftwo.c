#include <stdio.h>

int main()
{
    int a, b, s;
    printf("Enter the 1st number: ");
    scanf("%d",&a);

    printf("Enter the 2nd number: ");
    scanf("%d",&b);

    s=a+b;
    printf("The sum of two numbers is: %d",s);
    return 0;
}
