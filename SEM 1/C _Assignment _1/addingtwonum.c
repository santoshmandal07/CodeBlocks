#include <stdio.h>

int main()
{
    int a, b, s;
    printf("Enter two numbers: \n");
    scanf("%d%d",&a,&b);
    s = a + b; //adding the numbers
    printf("The sum of the two numbers is: %d",s);
    return 0;
}