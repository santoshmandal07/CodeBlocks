{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build C23",
            "type": "shell",
            "command": "gcc",
            "args": [
                "-std=c23",
                "${file}",
                "-o",
                "${fileBasenameNoExtension}.exe"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"]
        }
    ]
}
